
var $existingConfigsHolder = 
  $('ul.collapsible li.active')
    .find('.existing-configs-holder');

$existingConfigsHolder
  .find('tr[data-config-id="<%= @outbound_event_config.id %>"]')
  .fadeOut(300, function() {
    $(this).remove();
  });
